#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strncpy - copy string
 * @dest: destination to copy in
 * @src: source from where to cpy
 * @n : amount to cpy
 * Return: chare value
 */
char *_strncpy(char *dest, char *src, int n)
{
	return (_strncpy(dest, src, n));
}
