#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _memcpy - copy memory
 * @dest : destination memory
 * @src: source memory
 * @n : amount of memory
 * Return: chare value
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (_memcpy(dest, src, n));
}
