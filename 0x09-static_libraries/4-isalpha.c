#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isalpha - check is alpha
 * @c: int to check
 * Return:int value
 */
int _isalpha(int c)
{
	return (_isalpha(c));
}
