#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit - check is it digit
 * @c : the  int to check
 * Returnx: int value
 */
int _isdigit(int c)
{
	return (_isdigit(c));
}
