#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _memset - memory set
 * @s: string to set
 * @b: char
 * @n : unsigned int
 * Return:char value
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (_memset(s, b, n));
}
