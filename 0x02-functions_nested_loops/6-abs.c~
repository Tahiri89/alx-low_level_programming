#include "main.h"

/**
 * print_sign - main function
 *
 * @n: character to compare
 *
 * Return: 1 if positive, 0 if zero, -1 if negative
 *
 */
int _abs(int)
{
	if (int <= 0)
	{
		int = int * (-1);
		_putchar(int);
	}
	else
	{
		_putchar(int);
	}
}
